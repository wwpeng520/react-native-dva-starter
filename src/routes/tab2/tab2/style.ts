import { ViewStyle, TextStyle } from 'react-native';

export interface IDemoScene1Style {
  container: ViewStyle,
}

export default {
  container: {
    position: 'absolute',
    top: 0,
    left: 0,
    bottom: 0,
    right: 0,
    backgroundColor: '#fff',
  },
};